services:
  - type: web
    name: arvind-portfolio
    env: python
    buildCommand: pip install -r chatbot/requirements.txt
    startCommand: cd chatbot && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
